# The FastAPI server origin (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# The FastAPI route that accepts POST { "query": "..." }.
VITE_ANALYTICS_ENDPOINT=/api/query

# Set to true only when developing the frontend without a FastAPI server.
VITE_USE_MOCK_API=false
